# Operators in Python 

## What are Operators?

Operators are symbols used to perform operations on variables and values.

Example:

```python
a = 10
b = 5

print(a + b)
```

Output:

```txt
15
```

---

## Types of Operators in Python

1. Arithmetic Operators
2. Comparison Operators
3. Assignment Operators
4. Logical Operators
5. Membership Operators
6. Identity Operators

---

# 1. Arithmetic Operators

Used for mathematical calculations.

| Operator | Meaning        | Example |
|----------|----------------|---------|
|     +    | Addition       |  a + b  |
|     -    | Subtraction    |  a - b  |
|     *    | Multiplication |  a * b  |
|     /    | Division       |  a / b  |
|     //   | Floor Division |  a // b |
|     %    | Modulus        |  a % b  |
|    **    | Power          |  a ** b |

Example:

```python
a = 10
b = 3

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a // b)
print(a % b)
print(a ** b)
```

Output:

```txt
13
7
30
3.333333
3
1
1000
```

---

# 2. Comparison Operators

Used to compare values.

| Operator | Example |
|-----------|----------|
| == | a == b |
| != | a != b |
| > | a > b |
| < | a < b |
| >= | a >= b |
| <= | a <= b |

Example:

```python
x = 10
y = 20

print(x == y)
print(x != y)
print(x < y)
```

Output:

```txt
False
True
True
```

---

# 3. Assignment Operators

Used to assign values.

```python
x = 10

x += 5
print(x)

x -= 3
print(x)
```

Output:

```txt
15
12
```

---

# 4. Logical Operators

Used with conditions.

| Operator | Meaning |
|-----------|----------|
| and | Both conditions True |
| or | Any condition True |
| not | Reverse condition |

Example:

```python
age = 20

print(age > 18 and age < 30)
print(age > 50 or age < 25)
print(not(age > 18))
```

---

# 5. Membership Operators

Checks if a value exists.

```python
name = "Python"

print("P" in name)
print("x" not in name)
```

Output:

```txt
True
True
```

---

# 6. Identity Operators

Checks memory location.

```python
x = [1,2]
y = x

print(x is y)
print(x is not y)
```

Output:

```txt
True
False
```

---

## Practice Questions

1. Perform all arithmetic operations on two numbers
2. Compare two numbers
3. Use logical operators with age conditions
4. Check if a letter exists in a word

---

## What I Learned Today

✅ Arithmetic operators  
✅ Comparison operators  
✅ Logical operators  
✅ Membership operators  
✅ Identity operators
